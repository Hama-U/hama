使い方
1 : Desktopかどこかにフォルダを保存
2 : コマンドライン上でフォルダの中に移動
3 : python main.pyを実行
4 : ライブラリが見つからないエラーが出たらコマンドライン上で%pip3 install ライブラリ名でインストール
5 : dataフォルダにcsvファイルが3つ保存されていれば実行成功
6 : main.pyの冒頭のdefineの値を変えて実行する

※グラフの出力は最後のplt.show()をコメントアウトすれば消える